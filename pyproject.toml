[project]
name = "debb"
version = "0.1.0"
description = "AI-assisted production debugging system"
requires-python = ">=3.12"
dependencies = [
    "code-review-graph>=2.3.2",
]

[dependency-groups]
dev = [
  "code-review-graph>=2.3.0",
]

[tool.uv.workspace]
members = [
  "evidence_gate",
]
